package p2p

// Peer represents a remote node in the cluster
type Peer interface {

}

// Transport represents a TCP connection to handle 
// connection between the nodes in the cluster
// from (TCP, UDP, Websocket, ...)
type Transport interface {
	
}